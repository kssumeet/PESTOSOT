# ── PESTOSOT web — production env (set these in Vercel project settings) ──

# Public URL of the deployed API (Railway/Render). No trailing slash.
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# Canonical public site URL (used for SEO metadata, sitemap, canonical, OG).
NEXT_PUBLIC_SITE_URL=https://your-frontend.vercel.app
